{
  "name": "AceSpace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the space"
    },
    "description": {
      "type": "string",
      "description": "Description or topic of the space"
    },
    "creator_id": {
      "type": "string",
      "description": "ID of the student who created the space"
    },
    "join_code": {
      "type": "string",
      "description": "Unique code for others to join"
    },
    "is_active": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "name",
    "creator_id",
    "join_code"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "data.app_role": "student"
      }
    },
    "read": {
      "user_condition": {
        "data.app_role": "student"
      }
    },
    "update": {
      "$and": [
        {
          "user_condition": {
            "data.app_role": "student"
          }
        },
        {
          "data.creator_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$and": [
        {
          "user_condition": {
            "data.app_role": "student"
          }
        },
        {
          "data.creator_id": "{{user.id}}"
        }
      ]
    }
  }
}